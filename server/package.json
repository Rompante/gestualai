{
  "name": "gestualai-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API backend do GestualAI — auth, perfis, histórico e serviço de modelo/vocabulário. Nunca recebe imagem/vídeo.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
